import React from 'react';

const BookInfo = ({ title, genre, format, releaseDate }) => {
  return (
    <div>
      <h2>{book.title}</h2>
      <h4>{book.genre}</h4>
      <h4>{book.formar}</h4>
    </div>
  )
}

export default BookInfo;
